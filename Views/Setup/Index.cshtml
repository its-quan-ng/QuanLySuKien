@{
    ViewData["Title"] = "Setup Admin Account";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-danger text-white">
                    <h3 class="mb-0">
                        <i class="bi bi-shield-lock"></i> Setup Admin Account
                    </h3>
                </div>
                <div class="card-body">
                    @if (ViewBag.Message != null)
                    {
                        <div class="alert @(ViewBag.Success ? "alert-success" : "alert-danger")">
                            <strong>@(ViewBag.Success ? "Success!" : "Error!")</strong> @ViewBag.Message
                        </div>
                    }

                    <p class="lead">Click nút bên dưới để tạo tài khoản Admin mặc định:</p>

                    <div class="card bg-light mb-3">
                        <div class="card-body">
                            <h5 class="card-title">Thông Tin Admin:</h5>
                            <ul class="list-unstyled mb-0">
                                <li><strong>Email:</strong> admin@@eventhub.com</li>
                                <li><strong>Password:</strong> Admin@@123</li>
                                <li><strong>Role:</strong> Admin</li>
                            </ul>
                        </div>
                    </div>

                    <form asp-action="CreateAdmin" method="post">
                        <button type="submit" class="btn btn-danger btn-lg w-100">
                            <i class="bi bi-plus-circle"></i> Create Admin Account
                        </button>
                    </form>

                    <hr />

                    <div class="text-center">
                        <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-house"></i> Back to Home
                        </a>
                    </div>
                </div>
            </div>

            <div class="alert alert-info mt-3">
                <i class="bi bi-info-circle"></i>
                <strong>Lưu ý:</strong> Trang này chỉ dùng để setup admin lần đầu. Sau khi tạo xong, bạn có thể xóa controller này hoặc thêm security check.
            </div>
        </div>
    </div>
</div>
